
# stage form game
model:
	fields:
		modelId:
		info:
			type: modelInfo
		curves:
		panes:
		vars:
		sequences:
		shocks:
		scenarios:
	otherFields: FALSE
	typeByFieldName: TRUE

modelInfo:
	fields:
		label:
		tags:
		descr:
	otherFields: TRUE

modelId:
	parentType: string

curves:
	fieldType: curve

panes:
	fieldType: panel

vars:
	fieldType: var

sequences:
	fieldType: sequence

shocks:
	fieldType: shock

scenarios:
	fieldType: scenario


curve:

panel:

var:

sequence:

shock:

scenario:
	fields:
		init:
		axis:
